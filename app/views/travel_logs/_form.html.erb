<%= form_with(model: travel_log) do |form| %>
  <% if travel_log.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(travel_log.errors.count, "error") %> prohibited this travel_log from being saved:</h2>

      <ul>
        <% travel_log.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :trip_name, style: "display: block" %>
    <%= form.text_field :trip_name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :travel_log, style: "display: block" %>
    <%= form.text_field :travel_log, class: "form-control col-12" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary btn-block col-12 mt-3"%>
  </div>
<% end %>
